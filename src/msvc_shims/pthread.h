/* Placeholder for pthread.h on MSVC */
#ifndef PAPAGAIO_PTHREAD_SHIM_H
#define PAPAGAIO_PTHREAD_SHIM_H

/* QuickJS includes pthread.h but doesn't always use it. 
   On Windows/MSVC, we don't have pthreads by default. */

#endif
